language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-autobahn' ]; then cd .. && eval "mv $currentfolder generator-autobahn" && cd generator-autobahn; fi

